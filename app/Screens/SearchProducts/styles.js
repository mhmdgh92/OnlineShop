import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    emptyContainer: { height: '100%', alignItems: 'center' },
    emptyView: { height: '70%', justifyContent: 'center', alignItems: 'center' },
    container: { alignItems: 'center' },
    innerView: { height: '100%', width: '95%' },
    flatList: { width: '100%' },
    marginBottom: { height: '16%' }
});