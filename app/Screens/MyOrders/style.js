import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: { alignItems: 'center' },
    emptyContainer: { height: '100%', width: '100%' },
    emptyView: { height: '75%', justifyContent: 'center', alignItems: 'center' }
});